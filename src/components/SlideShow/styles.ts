import styled from 'styled-components'

export const Slide = styled.div`
    position: relative;
    width: 300px;
    overflow: hidden;
    img {
        position: relative;
        z-index: 2;
        width: 100%;
    }
`
