import { styled } from 'styled-components'

export const CarrosselContainer = styled.div`
    display: flex;
    position: absolute;
    top: 72px;
    right: 22px;
    width: 85%;
    z-index: 1;
    column-gap: 8px;

    img {
        width: 100%;
    }
`
